# ===== RAG / Agent 框架 =====
langchain>=0.3.23,<0.4
langchain-core>=0.3.51,<0.4
langchain-community>=0.3.23,<0.4
langchain-openai>=0.3.12,<0.4

# ===== 向量数据库 / 嵌入 / 检索 / 重排 =====
chromadb==0.5.23
# 混合检索稀疏路：BM25 + 字符 bigram 分词
rank-bm25==0.2.2
# 嵌入 + CrossEncoder 重排（bge-reranker-base 由 scripts/download_reranker.py 单独下载权重）
sentence-transformers==3.4.1
# torch 建议安装 CPU 版以减小体积：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# ===== PDF 生成（说明书语料，数据初始化用）=====
reportlab==4.2.5

# ===== 后端 =====
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-dotenv==1.0.1
pydantic==2.11.4

# ===== 工具 =====
httpx==0.28.1
pytest==8.3.5
# 模型下载兜底（HuggingFace / hf-mirror 不可达时使用 ModelScope）
modelscope>=1.39.0